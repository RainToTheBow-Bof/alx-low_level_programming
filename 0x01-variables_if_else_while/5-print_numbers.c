#include <stdio.h>
#include <ctype.h>

/**
 * main - Entry point
 * Description: Printing the alphabets in lowercase
 * Return: Always 0 (Success)
 */
int main(void)
{
	int j;

	for (j = 0; j < 10; j++)
		printf("%d", j);
	return (0);
}
